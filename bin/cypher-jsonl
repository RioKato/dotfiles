#!/bin/bash
set -euo pipefail

jq -Rs '{statement: .}' |
curl -sS \
    -u neo4j:password \
    -H 'Content-Type: application/json' \
    -H 'Accept: application/jsonl' \
    http://localhost:7474/db/neo4j/query/v2 \
    -d @- |
jq -c 'select(."$event" == "Record") | ._body[0]'
